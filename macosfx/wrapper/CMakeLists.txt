project(FxPlugHangingTestWrapper)


set(FXPLUGIN_NAME "FxPlug Hanging Test")
set(FXPLUGIN_NAMESPACE "FxPlug Hanging")
set(FXPLUGIN_ID "${PROJECT_APP_ID}")
set(FXPLUGIN_LICENSE_NAME "FxPlug Hanging")

set(PROJECT_APP_WRAPPER "${PROJECT_NAME}")

set (APPLICATION_ROOT "/Applications")
set (PLUGIN_FX_ROOT "/Library/Application Support/Final Cut Pro/Templates.localized/Effects.localized/Test/${FXPLUGIN_NAMESPACE}")
set (PLUGIN_FX_ROOT_DATA "/Library/Application Support/FxPlugHangingTest/Data")
set (PLUGIN_BUNDLE_DATA "${PROJECT_APP_WRAPPER}")
set (PLUGIN_BUNDLE_INSTALATION_PATH "${APPLICATION_ROOT}/${PROJECT_APP_WRAPPER}.app")

set(INSTALLER_DIR ${CMAKE_BINARY_DIR}/${FXPLUGIN_ID}/installer)
set(PREP_INSTALLER_DIR ${INSTALLER_DIR})
set(PREP_INSTALLER_DATA_DIR ${PREP_INSTALLER_DIR}/packages/${FXPLUGIN_ID}/data)

set(INSTALLER_APP_NAME ${PROJECT_NAME}Installer-${DEHANCER_INSTALLER_VERSION}${DEHANCER_BUILD_SUFFIX})

message(STATUS "PLUGIN DEHANCER_PLUGIN_BUNDLE_DATA: ${DEHANCER_PLUGIN_BUNDLE_DATA}")
message(STATUS "PLUGIN INSTALLER_APP_NAME: ${INSTALLER_APP_NAME}")
message(STATUS "PLUGIN PATH: ${PROJECT_PLUGIN_PATH}")
message(STATUS "PLUGIN INSTALLER DIR: ${PREP_INSTALLER_DIR}")
message(STATUS "PLUGIN INSTALLER DATA DIR: ${PREP_INSTALLER_DATA_DIR}")

include(CMakeLists.in)
add_dependencies(${PROJECT_NAME} FxPlugHangingTestFxPlug)
